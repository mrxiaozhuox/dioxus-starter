[application]

# dioxus project name
name = "dioxus-starter"

# default platfrom
# you can also use `dioxus serve/build --platform XXX` to use other platform
# value: web | desktop
default_platform = "web"

# Web `build` & `serve` dist path
out_dir = "dist"

# resource (static) file folder
asset_dir = "public"

[web.app]

# HTML title tag content
title = "Dioxus Starter | opinionated dioxus template"

[web.watcher]

watch_path = [
    "src", 
    "public", 
    "Dioxus.toml",
]

index_on_404 = true

# include `assets` in web platform
[web.resource]

# CSS style file
style = []

# Javascript code file
script = [
    "/assets/tailwind/tailwind.min.js",
    "/assets/tailwind/tailwind.config.js",
]

[web.resource.dev]

# Javascript code file
# serve: [dev-server] only
script = []

[plugin]

available = true

loader = [
    "/Users/mrxzx/Development/DioxusLabs/plugin-dev/dioxus-binaryen-plugin/"
]

[plugin.dioxus-binaryen]

optimize = true